{
  "name": "offlinehub",
  "version": "1.0.0",
  "description": "Offline-first social app with walkie-talkie, local chat, and file sharing",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "install-all": "npm install",
    "dev": "concurrently \"npm --workspace=frontend run dev\" \"npm --workspace=backend run dev\"",
    "build": "npm --workspace=frontend run build && npm --workspace=backend run build",
    "start": "npm --workspace=backend run start",
    "test": "npm --workspace=frontend run test && npm --workspace=backend run test",
    "lint": "npm --workspace=frontend run lint && npm --workspace=backend run lint"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
